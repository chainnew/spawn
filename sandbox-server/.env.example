# Sandbox Server Configuration

# Server port
PORT=3080

# Podman container name (create with: podman run -d --name spawn-sandbox -it ubuntu:22.04 sleep infinity)
CONTAINER_NAME=spawn-sandbox

# Workspace path inside container
WORKSPACE_PATH=/workspace

# OpenRouter API key for LLM proxy (optional)
OPENROUTER_API_KEY=sk-or-v1-xxx

# xAI API key for Grok (optional)
XAI_API_KEY=xai-xxx
